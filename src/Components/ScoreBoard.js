import React, { Component } from 'react';
import {Card} from "semantic-ui-react";
export default class ScoreBoard extends Component{
    render() {
        return(
            <Card header = 'Money collected' description = '2000'/>
        )
    }
}